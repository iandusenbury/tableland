import { connect } from 'react-redux'
import { bindActionCreators } from 'redux'
import { fetchCoordinates } from '../../actions'
import PlacesExample from '../../components/placesExample'

export default connect(null, null)(PlacesExample)
